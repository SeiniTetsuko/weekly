下面是一版可以直接放到文档里的应用更新汇总说明。

Todo 日报回写应用更新汇总说明

1. 本次更新目标

本次更新主要是为了提升 Todo 日报回写应用的兼容性、统计准确性和异常可追踪性。

更新后，应用可以从各项目日报中自动解析 TodoList任务进展，将用户填写的进展、承诺完成时间、风险等级回写到 Todo 系统，并根据填写情况自动更新任务标题标签、发送格式错误提醒、生成任务回收报告。

⸻

2. 解析能力更新

2.1 支持多种任务区域标题

原先只识别固定标题：

平台项目重要任务进展

现在兼容以下标题：

TodoList任务进展
TodoList 任务进展
平台项目重要任务进展
WBS任务进展
WBS 任务进展

这样不同部门日报模板中只要使用上述标题，应用都可以进入 Todo 任务解析区域。

⸻

2.2 支持多种 Todo ID 写法

原先只兼容部分纯数字格式，现在支持：

TODO#51004
WBS#51004
【TODO#51004】
【WBS#51004】
【51004】

如果任务标题中同时存在 Todo ID 和子任务 ID，例如：

🚀 【项目A】&【任务B】&【TODO#51004】&【30177】

系统会优先识别 TODO#51004 作为 Todo 任务 ID。

⸻

2.3 支持宽松日期格式

用户填写承诺完成时间时，现在支持更宽松的日期格式，并会统一转换为 YYYY-MM-DD。

支持示例：

2026-5-4    -> 2026-05-04
2026-05-04  -> 2026-05-04
2026/5/4    -> 2026-05-04
2026.5.4    -> 2026-05-04
20260504    -> 2026-05-04

同时系统会校验真实日期，例如：

2026-2-30

仍会被判定为格式错误。

⸻

3. 回写规则更新

3.1 进展回写规则

进展回写按 owner 维度执行。

日报中的结构示例：

### @张三
🚀 【项目A】&【任务B】&【TODO#51004】
<今日进展描述>:
  - 完成接口联调

系统会使用 @张三 匹配 Todo owners 中的用户，并用该用户的 x-user-guid 调用 insertProgress 接口写入进展。

如果同一个 Todo 有多个 owner，每个 owner 会分别写入自己的进展。

⸻

3.2 承诺完成时间 / 风险等级回写规则

promiseDate 和 riskLevel 仍然按照 TodoList 接口返回的 owners 顺序顺延。

规则如下：

promiseDate：按 owner 顺序，取第一个填写了合法承诺完成时间的 owner。
riskLevel：按 owner 顺序，取第一个填写了合法风险等级的 owner。

两个字段可以来自不同 owner。

示例：

owners 顺序：张三 -> 李四 -> 王五
张三：未填写承诺完成时间，填写风险等级 = 中
李四：填写承诺完成时间 = 2026-05-04，填写风险等级 = 高

最终回写结果：

promiseDate 取李四：2026-05-04
riskLevel 取张三：middle

如果字段值与 Todo 当前值一致，则不会重复传入 update payload。

⸻

4. title 标签规则更新

本次更新后，任务标题中的状态标签统一由系统维护。

支持的标签包括：

【X次未更新】
【格式错误】
【回写失败】

4.1 标签清洗规则

在生成新 title 前，系统会先清除旧标签，只保留真实任务名。

例如：

【2次未更新】【格式错误】框架搭建调试

会先还原为：

框架搭建调试

再根据当天状态重新生成标签。

因此不会出现标签重复叠加的问题，例如：

【1次未更新】【2次未更新】【3次未更新】任务A

⸻

4.2 title 标签只看第一位 owner

对于多 owner 任务，title 标签只以 TodoList 接口返回的第一位 owner 的填写状态为准。

也就是说：

owners = [张三, 李四, 王五]

title 标签只看张三当天是否完整填写、是否格式正确。

⸻

4.3 未更新标签规则

如果第一位 owner 的以下任一字段未填写：

进展描述
承诺完成时间
风险等级

则 title 加：

【X次未更新】

其中 X 根据 lastModifyDate 到当前日期之间的工作日天数计算，周六、周日不计入。

⸻

4.4 格式错误标签规则

如果第一位 owner 的承诺完成时间或风险等级存在格式错误，则 title 加：

【格式错误】

如果同时存在字段未填写和格式错误，则两个标签同时存在：

【X次未更新】【格式错误】任务名称

⸻

4.5 回写失败标签规则

如果 TodoList 接口中存在某任务，但当天日报中没有找到对应 Todo ID，则该任务视为回写失败，title 加：

【回写失败】

如果后续某次运行中该任务成功在日报中被解析到，则系统会自动清除：

【回写失败】

⸻

4.6 完整更新时清除标签

如果第一位 owner 当天满足：

进展描述已填写
承诺完成时间已填写且格式正确
风险等级已填写且格式正确

则系统会清除该任务 title 中已有的：

【X次未更新】
【格式错误】
【回写失败】

只保留真实任务名称。

⸻

5. 报告统计规则更新

报告统计与 title 回写规则不同。

title 标签：只看第一位 owner
报告统计：精确到每个 owner-task

也就是说，多 owner 任务中，即使第一位 owner 完整更新，其他 owner 如果漏填字段，仍然会在报告中被统计为未更新。

⸻

6. 报告内容结构

报告标题：

YYYY-MM-DD 项目管理任务进展更新情况

报告正文包括：

任务更新情况汇总
未更新人员情况
格式错误人员情况
更新失败人员情况

⸻

6.1 任务更新情况汇总

统计字段包括：

总任务数：TodoList 拉到的任务总数
涉及 owner 总数：TodoList 中涉及的 owner 去重数量
成功更新：进展 / 时间 / 风险均有填写且格式正确的 owner-task 数量
未更新：进展 / 时间 / 风险任一未填写的 owner-task 数量
格式错误：承诺完成时间 / 风险等级任一格式错误的 owner-task 数量
失败：TodoList 有但日报未找到对应 Todo ID 的任务数量

⸻

6.2 未更新人员情况

表格字段：

负责人 | 部门 | 任务Code | 任务名称 | 未更新字段 | 未更新次数

其中任务名称会自动去掉标签，例如：

【2次未更新】【格式错误】框架搭建调试

报告中显示为：

框架搭建调试

⸻

6.3 格式错误人员情况

表格字段：

负责人 | 部门 | 任务Code | 任务名称 | 错误字段 | 填写值

示例：

张三 | AIX/AIX2 | WBS#51004 | 框架搭建调试 | 承诺完成时间 | 2026-2-30

⸻

6.4 更新失败人员情况

表格字段：

负责人 | 部门 | 任务Code | 任务名称 | 失败原因

主要失败原因包括：

TodoList中存在该任务，但日报未找到对应任务ID
新增进展失败
字段/title 更新失败
回写失败标签更新失败

⸻

7. 格式错误提醒卡片更新

当用户填写的承诺完成时间或风险等级格式错误时，系统会向该 owner 发送个人提醒卡片。

提醒对象：

日报 h3 @人名匹配到的 Todo owner

提醒内容包括：

项目 ID
项目名称
任务 Code
任务名称
错误字段
用户填写值
正确格式

并提供“前往项目中心”按钮。

⸻

8. 典型流程示例

TodoList 中有任务：

WBS#51004 框架搭建调试
owners = [张三, 李四]

日报填写：

张三：
进展：完成接口联调
承诺完成时间：2026-5-4
风险等级：中
李四：
进展：补充日报解析逻辑
承诺完成时间：空
风险等级：高

系统处理结果：

1. 张三、李四各自写入 insertProgress。
2. promiseDate 按 owner 顺序取张三：2026-05-04。
3. riskLevel 按 owner 顺序取张三：middle。
4. title 标签看第一 owner 张三：
   张三完整填写且格式正确，因此 title 清除旧标签。
5. 报告统计：
   张三计入成功更新。
   李四计入未更新，未更新字段为：承诺完成时间。

⸻

9. 本次更新后的应用价值

本次更新后，应用相比旧版本主要提升了：

1. 模板兼容性更强
2. Todo ID 识别更稳定
3. 用户日期填写更宽松
4. 多 owner 任务处理规则更清晰
5. title 标签不会重复叠加
6. 任务名称在报告中更干净
7. 回写失败任务可追踪
8. 报告统计可以精确到每个 owner-task
9. 格式错误可以及时个人提醒

整体上，应用从“简单日报回写脚本”升级为一个更完整的：

Todo 任务填写回收
任务状态标记
异常提醒
回收质量统计

的自动化工具。